---
description: 日志与可观测性约定：只在关键节点记录，避免通篇流水账
globs: **/*.{ts,svelte}
alwaysApply: false
---

# Logging And Observability

- 日志只加在关键节点：跨进程/跨包/跨运行时边界、外部 SDK 或 shell 命令调用、用户触发的高价值操作、失败分支、不可见的阻塞/取消/降级路径。
- 不要为每个普通函数、普通状态同步、UI 展开/折叠、纯计算 helper、成功的内部中间步骤写流水账日志。
- 一次用户操作最多保留少量可串联日志：入口、关键外部调用、最终成功或失败。能通过上层日志判断的下层细节不要重复打印。
- 日志内容必须结构化并脱敏：记录路径、操作名、耗时、错误码、大小、候选次数等诊断字段；禁止记录文件正文、base64 payload、token、密钥、完整环境变量。
- 新增 logger 前先确认是否已有通用实现；优先复用共同依赖层能力，不在 app 和 package 中复制实现。
