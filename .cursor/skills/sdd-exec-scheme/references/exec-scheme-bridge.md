# Exec Scheme Bridge（模板）

嵌入任务 Spec 一节即可；**仅三段**，不增章节。

## 1. 改动依赖范围内的能力与代码现实

**范围**：仅 **本次实现会调用、或必须修改/扩展** 的仓库能力。不写「相关但正交」的盘点。

| 能力（将被触及） | 现状（够用 / 需扩 / 缺） | 证据（路径 `:` 符号） |
|------------------|---------------------------|------------------------|
| | | |

## 2. 外部依赖：包与本任务用到的精确 API

| 包（版本来源） | 本任务依赖的具体 API（符号 / 方法 / 类型） | 备注 |
|----------------|---------------------------------------------|------|
| | | |

## 3. 设计契约

**技术文档出处**：`<文档路径或标题> §… / 章节名`

**契约正文**（可与原文一致或已改写；改写须在下表说明）：

```typescript
// 或伪代码 / 接口草案 —— 保持最小表面
```

**相对技术文档的增量说明**（无改写则写「与原文一致」）：

| 项目 | 说明 |
|------|------|
| 沿用 | |
| 相对原文的改写（若有） | 原文怎样 → 现契约怎样 → **依据哪条代码现实** |
