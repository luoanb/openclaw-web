---
description: 编辑前端 UI 时遵循 Bits UI 设计索引与 Figma 约定
globs: "**/*.svelte,**/app.css,**/globals.css,**/*.tsx,**/*.jsx"
alwaysApply: false
---

# UI — Bits UI 设计为准

在修改或新增**页面、布局、组件样式、主题与交互**时：

1. 打开并对照 **`docs/design/BitsUI.md`**（Bits UI kit 节点索引与直达 Figma 链接）。
2. 需要核对具体控件状态、间距、色值时，用文档中的 `node-id` 链接在 Figma 中定位，避免凭感觉偏离设计体系。
3. 与 `AGENTS.md`「页面与设计（项目约定）」冲突时，以 **`AGENTS.md` + 本仓库最新设计文档** 为准；若设计文档过时，应在同一变更中更新 `docs/design/BitsUI.md` 或相关 spec 并简要说明。
